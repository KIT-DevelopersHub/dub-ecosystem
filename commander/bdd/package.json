{
  "name": "@dub/commander-bdd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Commander acceptance specs — executable BDD (Gherkin) that drives the REAL daemon + commander-service + app-registry over HTTP/SSE. Defines what 'クリア' means for Commander phases 1-3. Never deployed.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dub/commander-phases": "workspace:*",
    "@dub/db": "workspace:*",
    "@dub/types": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
