name = "dub-fe2-app-shell"
compatibility_date = "2026-08-01"

# FE2 admin SPA — Cloudflare Workers static assets (theme5 4-4).
# The API base is baked at build time via VITE_API_BASE_URL (the api-gateway URL).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
