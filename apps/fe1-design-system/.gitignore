# Built static Storybook (regenerated by `pnpm --filter @dub/ui build-storybook`).
storybook-static/

# Playwright visual-regression run artifacts (NOT the baselines).
# Baselines live in visual/__screenshots__/ and ARE committed.
visual/.output/
visual/.report/
visual/test-results/
