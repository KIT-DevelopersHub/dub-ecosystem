{
  "name": "@dub/usage-meter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "usage-meter — free-tier usage collector + billing-guard alerts. A DO-alarm-driven daily collector (no cron) that snapshots Cloudflare (GraphQL Analytics) and Resend (mail send-log COUNT) usage against the free-tier ceilings, serves GET /usage/summary, and alerts admins at warn(70%)/critical(90%).",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dub/auth-client": "workspace:*",
    "@dub/db": "workspace:*",
    "@dub/errors": "workspace:*",
    "@dub/http": "workspace:*",
    "@dub/observability": "workspace:*",
    "@dub/types": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
