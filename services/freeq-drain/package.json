{
  "name": "@dub/freeq-drain",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "freeq-drain — single cron-driven aggregated @dub/freeq drain worker (one cron slot, topic-aware routing, default-defer so foreign/unknown topics are never mis-acked).",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dub/freeq": "workspace:*",
    "@dub/observability": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
