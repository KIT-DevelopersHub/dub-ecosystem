{
  "name": "@dub/member-service",
  "version": "0.1.0",
  "private": true,
  "description": "DevHub (Dub) member-service — 運営メンバー管理 (invite status + team membership; the GUI replacement for the hand-maintained 組織図 PDF). Cloudflare Worker + Hono.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dub/auth-client": "workspace:*",
    "@dub/db": "workspace:*",
    "@dub/errors": "workspace:*",
    "@dub/http": "workspace:*",
    "@dub/observability": "workspace:*",
    "@dub/types": "workspace:*",
    "hono": "^4.6.14"
  },
  "peerDependencies": {
    "@cloudflare/workers-types": "^4"
  }
}
