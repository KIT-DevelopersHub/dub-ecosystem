{
  "name": "@dub/mail-gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "mail-gateway service (#15) — thin mail adapter: outbound send (managed provider, SES暫定) with idempotent send-log, inbound normalization (Cloudflare Email Routing -> Worker), event publish (mail.message.*), publishAudit.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dub/types": "workspace:*",
    "@dub/errors": "workspace:*",
    "@dub/events": "workspace:*",
    "@dub/db": "workspace:*",
    "@dub/auth-client": "workspace:*",
    "@dub/observability": "workspace:*",
    "@dub/http": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
